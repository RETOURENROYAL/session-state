{
  "_comment": "VS Code User MCP config — copy to: C:\\Users\\mail\\AppData\\Roaming\\Code\\User\\mcp.json",
  "_note": "r3-matrix-control :8422 does NOT expose /mcp — omitted. github-mcp-server uses stdio, NOT SSE.",
  "servers": {
    "r3-chatlegs-primary": {
      "type": "sse",
      "url": "http://localhost:8420/mcp"
    },
    "r3-chatlegs-shadow": {
      "type": "sse",
      "url": "http://localhost:8421/mcp"
    },
    "github": {
      "_comment": "GitHub MCP Server via Docker stdio — gives Copilot Chat repos/issues/PRs/actions tools",
      "command": "docker",
      "args": [
        "run", "-i", "--rm",
        "-e", "GITHUB_PERSONAL_ACCESS_TOKEN",
        "ghcr.io/github/github-mcp-server"
      ],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${input:github_token}"
      }
    }
  }
}
